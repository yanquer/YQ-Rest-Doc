===================================
特殊符号
===================================

code::

  &     表示任务在后台执行，如要在后台运行
  &&    表示前一条命令执行成功时，才执行后一条命令
  |     表示管道，上一条命令的输出，作为下一条命令参数(输入)
  ||    表示上一条命令执行失败后，才执行下一条命令
  >     符号是指：将正常信息重定向


